import React from 'react';
import PageSkeleton from '@/components/PageSkeleton';

function FAQ(): JSX.Element {
    return (
        <PageSkeleton firstPage="Home" secondPage="Payment">
            FAQ
        </PageSkeleton>
    );
}

export default FAQ;
